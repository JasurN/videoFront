export const makeTwoStrToLowerCaseAndConcat = (firstName, lastName) => {
    return `${firstName.toLowerCase()}${lastName.toLowerCase()}`;
};

export const makeTwoStrToLowerCaseAndConcatAndAddSlash = (firstName, lastName) => {
    return `${firstName.toLowerCase()}/${lastName.toLowerCase()}`;
};

